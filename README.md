# LibFFmpeg
LibFFmpeg++ is a C++ wrapper that can load the shared FFmpeg libraries of different versions

# Build FFMpeg

This is the buildFFmpeg branch which contains the scripts that we use to build all the ffmpeg versions for testing.

